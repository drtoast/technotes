// http://api.jquery.com/category/utilities/

function utilities() {
    // iterate over arrays or objects
    $.each([52, 97], function(index, value) { 
      alert(index + ': ' + value); 
    });
    
}