# Disk Usage

    du
        -s (summarize)
        -c (grand total)
        -h (human readable)

# Examples

show human-readable disk usage summary for the directory "earbox.com"

    du -sh earbox.com


    df
        -h (human readable)